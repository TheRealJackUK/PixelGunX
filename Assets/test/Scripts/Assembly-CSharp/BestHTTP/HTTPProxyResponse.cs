using System.IO;

namespace BestHTTP
{
	public class HTTPProxyResponse : HTTPResponse
	{
		internal HTTPProxyResponse(HTTPRequest httprequest_1, Stream stream_2, bool bool_5, bool bool_6) : base(default(HTTPRequest), default(Stream), default(bool), default(bool))
		{
		}

	}
}
