using BestHTTP;
using System.IO;

namespace BestHTTP.WebSocket
{
	public class WebSocketResponse : HTTPResponse
	{
		internal WebSocketResponse(HTTPRequest httprequest_1, Stream stream_2, bool bool_7, bool bool_8) : base(default(HTTPRequest), default(Stream), default(bool), default(bool))
		{
		}

	}
}
