using BestHTTP;
using System.IO;

namespace BestHTTP.ServerSentEvents
{
	internal class EventSourceResponse : HTTPResponse
	{
		internal EventSourceResponse(HTTPRequest httprequest_1, Stream stream_2, bool bool_6, bool bool_7) : base(default(HTTPRequest), default(Stream), default(bool), default(bool))
		{
		}

	}
}
