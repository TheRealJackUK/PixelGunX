using BestHTTP.SignalR;

namespace BestHTTP.SignalR.Transports
{
	public class WebSocketTransport : TransportBase
	{
		public WebSocketTransport(Connection connection_0) : base(default(string), default(Connection))
		{
		}

	}
}
